# Terraform
terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# .terraform.lock.hcl is intentionally NOT ignored - it pins provider
# versions for reproducible `terraform init`, same idea as a lockfile.
# terraform/exam/ - scratch practice files, not part of the real project
terraform/exam/

# Ansible
ansible/kubeconfig-aws.yaml
ansible/inventory.ini
*.retry
